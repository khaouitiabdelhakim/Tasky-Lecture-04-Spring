server:
  port: 8081

spring:
  application:
    name: Tasky
  datasource:
    url: jdbc:mysql://localhost:3306/tasky-db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
